<div class="page-width">
  <div class="grid">
      <div class=section-header text-center">
          <h1>{{ page.title }}</h1>
      </div>

    {%  case page.content.size %}
      {%  when 150 %}
        The page has only 150 characters, so this should not take much time to read.
      {% when 350 %}
        We have bumped the page to 350 characters, but it should not take that much time.
      {% when 1000 %}
        We not have 1000 characters written, and it is going to take a few minutes to read everything carefully.
    {% endcase %}
      
        {% if page.content.size > 100 %}
        <div class="rte">
          {{ page.content }}
        </div>
        {% elsif page.content.size < 100 and page.content != blank %}
        <div class="rte">
          Our content writters are making final touches, and the page content will be available shortly.
        </div>
        {% else %}
        <div class="rte">
          The page content is currently pending. Please check again soon.
        </div>
        {% endif %}

  </div>
</div>
